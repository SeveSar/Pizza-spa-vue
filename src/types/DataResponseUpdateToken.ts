export default interface DataResponseUpdateToken {
  user_id: string;
  refresh_token: string;
  id_token: string;
  expires_in: string;
  project_id: string;
  token_type: string;
  access_token: string;
}
